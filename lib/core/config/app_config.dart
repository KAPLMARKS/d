import 'api_keys.dart' as keys;

class AppConfig {
  static const geminiApiKey = keys.geminiApiKey;
  static const voiceRssApiKey = keys.voiceRssApiKey;
  static const apphudApiKey = keys.apphudApiKey;
  static const appsFlyerDevKey = keys.appsFlyerDevKey;
  static const appsFlyerAppleAppId = keys.appsFlyerAppleAppId;
}
